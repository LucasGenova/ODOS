# ODOS

## Como rodar o projeto

Basta rodar os comandos `make` e `make run` no diretorio principal do projeto

### Obs: o padrão `#ifndef X; #define X; #end if \\X` nos arquivos .h impede definições repetidas